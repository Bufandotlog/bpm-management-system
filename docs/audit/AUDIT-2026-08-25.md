# LAPORAN AUDIT & REMEDIASI — Sistem BEM Astawidya
Tanggal: 25 Agustus 2026 | Scope: /var/www/html/bem

## A. Ringkasan Audit
- Engine final: MariaDB/MySQL-kompatibel. Bukti: docker-compose.yml (mariadb:10.11-jammy),
  Dockerfile.prod (pdo_mysql+mysqli), .env.example, live server MariaDB 11.8.6.
  PostgreSQL = dukungan sekunder yang masih aktif (28 referensi pgsql di 6 file) — TIDAK dihapus.
  SQLite ditolak: tidak ada driver di config ($allowedDrivers = mysql|pgsql); file database.sqlite
  di webroot = artefak legacy dari bug .env (sudah diperbaiki).
- Root cause insiden 23 Aug (php-error.log): baris orphan `DB_DATABASE=database/database.sqlite`
  di .env menimpa nama DB MySQL → aplikasi menjalankan ALTER TABLE gaya MySQL ke SQLite →
  error "no such table/column". Parser .env milik config/database.php bersifat first-wins,
  sehingga setelah duplikat dihapus, nilai yang benar (bem_astawidya) berlaku.

## B. Arsitektur & Source of Truth
- Source of truth struktur DB = LIVE database `bem_astawidya` (MariaDB 11.8.6 native systemd).
- databases/schema_mysql.sql = dokumen instalasi-fresh; diregenerasi DARI live DB (mariadb-dump --no-data).
- Live DB TIDAK diubah sama sekali (hanya SELECT/SHOW).

## C. Schema Mismatch (temuan STEP 3)
- 10 tabel ada di live DB tapi hilang dari schema lama: panitia_tetap, surat_templates,
  lampiran_pinjam, rundown_tempat, arsip_panitia, arsip_teks_mc, visi_misi, rundown_pj,
  rundown_keterangan, kontak.
- 25+ kolom hilang dari schema lama (arsip_surat.kegiatan_id/status_arsip/status_humas,
  kegiatan.waktu_pelaksanaan/tempat_pelaksanaan/kode_kegiatan/dll, notifikasi.judul,
  users.file_ttd/google_*/totp_*/last_*, anggota_bph.user_id, anggota_kementerian.user_id,
  arsip_rundown.kegiatan_id, berita.footnote/updated_at/views, lpj_dokumen.penutup,
  struktur_organisasi.created_by/is_active, pengaturan.updated_at, periode_kepengurusan.deskripsi/updated_at).
- Tabel beda-generasi total: kementerian, struktur_bph, short_links, user_sessions, kabinet,
  tempat_master, arsip_dokumentasi.
- Semua gap kini tercakup dalam schema_mysql.sql hasil regenerasi (36 tabel aktif + 3 deprecated + 3 views).

## D. Remediasi yang Dilakukan (STEP 4–6)
1. .env lokal dibersihkan (backup: .env.bak-2026-08-25): scaffold Laravel tak terpakai dihapus
   (diverifikasi 0 referensi), duplikat DB_DATABASE dihapus, hanya variabel yang dikenal sistem.
2. config/database.php: fix bug $dbSslMode (lokal vs $GLOBALS — sslmode pgsql kini benar-benar
   diterapkan). php -l PASS.
3. databases/schema_mysql.sql diregenerasi dari live DB via mariadb-dump --no-data
   (backup lama: schema_mysql.sql.bak-2026-08-25):
   - tanpa data produksi, tanpa credential;
   - komentar seed plaintext password (admin1234) hilang — schema kini structure-only;
   - 3 tabel deprecated (keterangan_master, penanggung_jawab_master, signatures) dipertahankan
     dengan marker DEPRECATED + dokumentasi penggantinya (revisi user #1);
   - MyISAM/latin1 TIDAK dikonversi — merepresentasikan live DB apa adanya (revisi user #2);
   - konversi opsional InnoDB/utf8mb4 dicatat sebagai hardening terpisah.
4. composer.json: TAMBAH ext-pdo_mysql, ext-pdo_pgsql dipertahankan (PostgreSQL masih didukung —
   revisi user #4). composer.lock disinkronkan. composer validate PASS (warning license saja).
5. Dokumentasi (README.md, docs/setup/INSTALL.md, docs/PANDUAN_LOKAL_DAN_ARSITEKTUR.md +
   salinan architecture/): PHP >= 8.1, MariaDB utama + PostgreSQL sekunder, cp .env.example .env,
   variabel DB_SSLMODE (bukan DB_SSL_MODE), instruksi buat akun superadmin pertama via SQL
   dengan hash asli terverifikasi password_verify(), peringatan schema_pgsql.sql belum sinkron.
6. Izin file: .env 644 (sempat 640 bufan:bufan → php-fpm www-data gagal baca → HTTP 500; sudah
   diperbaiki dan diverifikasi 200).

## E. Validasi (STEP 7)
- php -l seluruh project (non-vendor): ALL PASS.
- composer validate: valid.
- Smoke test via nginx+php-fpm: index/kontak/berita/berita-detail/kepengurusan/arsip-periode/
  daftar/detail-menteri = 200; admin/ = 302 (redirect login, benar); api/notifications.php = 401
  (auth required, benar); halaman admin subdirektori = 404 app-level route guard untuk request
  anonim (respons dibuat aplikasi, bukan nginx).
- logs/php-error.log tidak ada entri baru sejak perbaikan (entri terakhir 02:59, pra-fix).

## Isu tersisa (tidak disembunyikan)
1. Auto-migration runtime di includes/functions.php (~lines 227–560) memakai try{SELECT}→ALTER
   + catch kosong (exception suppression) — technical debt; butuh refactor terjadwal.
2. schema_pgsql.sql stale — ditandai di docs; PostgreSQL sekunder belum disinkronkan.
3. pdo_pgsql.so di host lokal rusak ("undefined symbol: pdo_dbh_ce") — warning tiap CLI PHP;
   tidak memengaruhi jalannya aplikasi (driver mysql). Fix host-level: reinstall php8.5-pgsql.
4. File sampah webroot — DIBERSIHKAN 25 Aug (approved user): database.sqlite + dir database/,
   .env.bak-1787482772, mobile_app/build/ (1.1 GB), test-results/, admin/node_modules.zip dihapus;
   2 file pribadi (surat undangan .docx + .xdp) dipindah ke ~/Documents/personal-docs/
   (keduanya masih tracked di git → akan muncul sebagai deletion saat commit berikutnya).
   scratch/ TIDAK dihapus: bem_lpj_manager.py = engine LPJ yang dipanggil buat-lpj.php:892
   dan arsip-lpj.php:75.
5. Fresh install kini tanpa seed admin — prosedur pembuatan akun pertama terdokumentasi di
   INSTALL.md (opsional: buat scripts/create-admin.php CLI di kemudian hari).
